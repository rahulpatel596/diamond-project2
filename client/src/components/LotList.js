import { useState, useEffect } from "react";
const axios = require("axios");

function LotList() {
  return (
    <div>
          <h1>Lot Lists</h1>
          <p>May be try Mongodb package instead of Mongoose</p>
          <p>Also create a mongodb for development not messing current prod DB</p>
          <p>{lot_name, company_name, lot_number, _id, lot_asort_details:{stuff which I already have in prod DB} }</p>
          <p>Create this Lot List component and list all lots and clicking on each should take it to </p>
    </div>
  );
}

export default LotList;
